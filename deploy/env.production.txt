# Production .env template for Manifold Apps.
# Create this as  /home/manifold/repositories/manifold-apps/.env  (App root, NOT public).
# Fill APP_KEY, DB_PASSWORD, and ADMIN_* before first use. Never commit the real .env.

APP_NAME="Manifold Apps"
APP_ENV=production
APP_KEY=base64:GENERATE_WITH__php_artisan_key_generate_--show
APP_DEBUG=false
APP_URL=https://apps.manifold.ro
APP_TIMEZONE=Europe/Bucharest

APP_LOCALE=ro
APP_FALLBACK_LOCALE=en
LOG_CHANNEL=stack
LOG_LEVEL=error

DB_CONNECTION=mysql
DB_HOST=127.0.0.1            # if "Connection refused", try: localhost
DB_PORT=3306
DB_DATABASE=manifold_apps
DB_USERNAME=manifold_apps
DB_PASSWORD=your-db-password

SESSION_DRIVER=file
SESSION_SECURE_COOKIE=false  # flip to true AFTER https works
CACHE_STORE=file
QUEUE_CONNECTION=sync

# Secret for the no-SSH migration endpoint  /_deploy/migrate?token=...
# Generate: php -r "echo bin2hex(random_bytes(24)).PHP_EOL;"
DEPLOY_TOKEN=generate-a-long-random-string

# Admin is seeded via deploy/production.sql; these are only used if you ever
# re-seed with artisan (not used in the no-shell flow).
ADMIN_NAME="Claudiu"
ADMIN_EMAIL=claudiu.man@gmail.com
ADMIN_PASSWORD=set-a-strong-password
